# Jenkins for Beginners

## Key pieces of CI
- Code packaging
- Clone repo and package
- Unit test
- Integration test
- Security checks

## Key pieces of CD
- Deploy the code to system
- Server, container, bare metal, on prem

## Deployment versus Delivery
- In Delivery, someone press the deployment button
- Delivery: Deploy the code manually
- Deployment: Code is automatically deployed after CI

# Why Jenkins
- Free

# Jenkins Administration

## Backup and Restore
- Plugin for backup
    - Manage plugin
    - Install "ThinBackup"
    - Configure backup settings
- Filesystem snapshots
- Shell script to back up Jenkins instance (Google for Jenkins backup script)

### What To Backup
- `$JENKINS_HOME`
    - `config.xml`
    - `/jobs`

## Monitoring

## Scaling

## Manage

# Jenkins Pipeline (Jenkinsfile)

## What is a Jenkinsfile
- Within stages you can have multiple stage
- `stage('dev')` vs `stage('uat')`
- In a stage you have steps

## Building a Jenkinsfile
- Named `Jenkinsfile`
- `agent` is a build agent
- Single stage, multi stage
    - Stage: `dev`, `uat`, `prod`
    - Stage involves some steps
    - Build -> All stages are built concurrently
- Steps are executed by Jenkins
    - `git`
    - `echo`

## CI Pipeline
- `tools {}` which compiler to use, like `jdk` or `maven`
- `environment {}` defines environment variable
- `steps {}`
    - `git`
    - `sh`
    - `echo`

## CD Pipeline
- 
- 


## Examples

### Build and push docker image
```python
pipeline {
    agent any

    environment {
        // Define Docker Hub variables
        DOCKER_CREDENTIALS_ID = 'dockerhub-credentials'
        DOCKER_IMAGE_NAME = 'your-dockerhub-username/your-image-name'
        GIT_REPO_URL = 'https://github.com/your-username/your-repository.git'
    }

    stages {
        stage('Clone Repository') {
            steps {
                // Clone the Git repository
                git url: "${GIT_REPO_URL}", branch: 'main'
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    // Build the Docker image
                    def image = docker.build("${DOCKER_IMAGE_NAME}:latest")
                }
            }
        }

        stage('Push Docker Image to Docker Hub') {
            steps {
                script {
                    // Login to Docker Hub and push the image
                    docker.withRegistry('', DOCKER_CREDENTIALS_ID) {
                        def image = docker.image("${DOCKER_IMAGE_NAME}:latest")
                        image.push()
                    }
                }
            }
        }
    }

    post {
        always {
            // Clean up any images to save space
            sh "docker rmi ${DOCKER_IMAGE_NAME}:latest || true"
        }
    }
}
```